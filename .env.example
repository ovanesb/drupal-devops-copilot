# Helper
# This command reads the .env file,
# ignores any comments,
# and sets the remaining key-value pairs as environment variables in the current shell.
# |-> export $(grep -v '^#' .env | xargs)

OPENAI_API_KEY=

# LLM provider and model
# optional, defaults to this anyway
# - openai
# - ollama
LLM_PROVIDER=

# LLM model to use, e.g. gpt-4o-mini, gpt-4o, gpt-3.5-turbo, etc.
# See https://platform.openai.com/docs/models for OpenAI models
# See https://ollama.com/docs/usage/models for Ollama models
LLM_MODEL=

# Core
LLM_REQUEST_TIMEOUT=
LLM_MAX_RETRIES=
LLM_BACKOFF_BASE_S=
LLM_PROMPT_MAX_CHARS=

# Ollama-specific
OLLAMA_HOST=
# If provider=


OLLAMA_NUM_PREDICT=
OLLAMA_NUM_CTX=
OLLAMA_KEEP_ALIVE=
OLLAMA_TEMPERATURE=
OLLAMA_TOP_P=
OLLAMA_TOP_K=
OLLAMA_REPEAT_PENALTY=


# Copilot settings
# Enable/disable various features
# optional debug to capture raw outputs
COPILOT_DEBUG_LLM=
COPILOT_FORCE_MANIFEST=


GIT_TARGET_BRANCH=

# JIRA settings
JIRA_API_TOKEN=
JIRA_EMAIL=
JIRA_BASE_URL=
JIRA_DEFAULT_ISSUE=

# JIRA transitions
# When MR opens (auto_dev already uses this)
JIRA_TRANSITION_ON_MR_OPEN=

# When AI says "changes requested" (used by ai_review_and_merge.py)
JIRA_TRANSITION_ON_CHANGES=

JIRA_TRANSITION_ON_QA_PASS=
JIRA_TRANSITION_ON_QA_FAIL=
JIRA_ON_QA_PASS_PRE=
JIRA_ON_QA_PASS_POST=

# When MR is merged into staging (used by ai_review_and_merge.py)
# Order matters: we'll apply the first available transition.
JIRA_TRANSITION_ON_MERGE=

# Resolve by jirat fled name:
JIRA_AC_FIELD_NAME=
JIRA_QA_FIELD_NAME=
# Get field key by name:
# curl -s -u "$JIRA_EMAIL:$JIRA_API_TOKEN" "$JIRA_BASE_URL/rest/api/3/field" | jq -r '.[] | select(.name=
JIRA_QA_FIELD_KEY=

SYSTEM_MESSAGE=
MERGE_RETRY_MAX=
MERGE_RETRY_INTERVAL=
GITLAB_BASE_URL=
GITLAB_PROJECT_PATH=
GITLAB_API_TOKEN=
JIRA_BROWSE_URL_PREFIX=
# user id
GITLAB_ASSIGNEE_ID=
# comma-separated ids
GITLAB_REVIEWER_IDS=
COPILOT_DISABLE_LLM=
COPILOT_REPO_PATH=
COPILOT_REPO_URL=
GITLAB_MR_LABELS=
COPILOT_DEFAULT_BASE=
AI_REVIEW_RETARGET_BRANCH=
COPILOT_DOCROOT=

# Project settings
# Append any additional instructions to the system prompt here.
DRUPAL_GENERATION_SYSTEM_PROMPT=
DRUPAL_REVIEW_SYSTEM_PROMPT=


# AWS
# EC2
# IP address or hostname
DEPLOY_HOST=
DEPLOY_USER=
EC2_USER=
# chmod 600
DEPLOY_SSH_KEY=
# deployment directory on the remote server
DEPLOY_WORKDIR=
# Base URL of the deployed site, e.g. http://1.1.1.1 or http://example.com
SITE_BASE_URL=
# Deployment script to run on the remote server.
DEPLOY_SCRIPT=
# EC2 host.
EC2_HOST=
# SSH user.
EC2_KEY=


# Drush alias URI for the remote Drupal site.
DRUSH_URI=